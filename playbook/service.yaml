apiVersion: v1
kind: Service
metadata:
  name: eapp-web-host-svc
  namespace: ethosverse
spec:
  selector:
    epod: eapp-web
  ports:
    - name: mv-web-http
      protocol: TCP
      port: 30010
      targetPort: 50601