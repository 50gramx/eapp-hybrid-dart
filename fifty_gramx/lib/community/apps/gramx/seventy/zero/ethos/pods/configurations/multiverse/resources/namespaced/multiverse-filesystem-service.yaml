apiVersion: v1
kind: Service
metadata:
  name: multiverse-filesystem-service
  namespace: ethosverse
spec:
  selector:
    epod: mv-fs
  ports:
    - name: mv-fs-control
      port: 5001
      targetPort: 5001
      nodePort: 30501